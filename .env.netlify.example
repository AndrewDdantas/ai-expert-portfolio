# Variáveis de Ambiente para Netlify Functions
# Copie este arquivo para .env na raiz do projeto para desenvolvimento local

# API Externa - OBRIGATÓRIO
API_URL=https://sua-api-externa.com
API_TOKEN=Bearer seu-token-secreto-aqui

# Node.js - OPCIONAL
NODE_VERSION=18

# ⚠️ IMPORTANTE: 
# - Este arquivo .env é apenas para desenvolvimento local com 'netlify dev'
# - Em produção, configure as variáveis no Netlify Dashboard
# - NUNCA faça commit do arquivo .env (já está no .gitignore)
