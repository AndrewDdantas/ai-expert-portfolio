# Frontend Configuration
# O frontend agora se conecta ao nosso backend local
# Não precisa mais de API_TOKEN ou API_URL aqui!

# URL do backend local (padrão: http://localhost:3001)
VITE_BACKEND_URL=http://localhost:3001

# ⚠️ NOTA: As credenciais da API ficam no arquivo server/.env
# Veja server/.env.example para configurar o backend